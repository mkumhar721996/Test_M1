{
  "name": "test-m1-slot-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  }
}
